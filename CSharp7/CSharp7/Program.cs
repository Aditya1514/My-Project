using System;

namespace CSharp7
{
    class Program
    {
        static void Main()
        {
            //reference parameters
            // int i = 0;
            // SimpleMethod(ref i);
            //  Console.WriteLine(i);

            //output parameters

            //int Total = 0;
            // int Product = 0;


            // Calculate(15, 14, out Total, out Product);
            // Console.WriteLine("Sum = {0} && Product = {1}", Total,Product);

            //parameter Array
            //paramsMethod(1, 2, 2, 25);
        }
           
        
            
        

    }
}
